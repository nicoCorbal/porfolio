---
import { ClientRouter } from 'astro:transitions';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonical?: string;
  lang: string;
  isMainPage?: boolean;
}

const { 
  title = 'OSIX - Innovation & Development',
  description = 'We create innovative solutions for your business. We specialize in mobile, web, and desktop development with cutting-edge technologies.',
  image = '/favicon.png',
  canonical = 'https://osix.tech',
  lang,
  isMainPage = false
} = Astro.props;	
---

<!DOCTYPE html>

<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, minimum-scale=1.0, user-scalable=yes" />

    <meta name="theme-color" content="#000000" />
    <meta name="mobile-web-app-capable" content="yes" />

    <title>{title}</title>
    <meta name="description" content={description} />

    <!-- Favicon base -->

    <link rel="icon" href="/favicon.ico" sizes="any" type="image/x-icon">
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="icon" type="image/png" sizes="96x96" href="/favicon-96x96.png">
    <link rel="icon" type="image/png" sizes="48x48" href="/favicon-48x48.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">

    <!-- SEO Meta Tags -->
    <meta name="robots" content="index, follow" />
    <meta name="author" content="OSIX" />
    <meta name="keywords" content="software development, web development, mobile development, desktop development, innovation, technology" />

    <!-- Canonical URL -->
    <link rel="canonical" href={canonical} />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={image} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonical} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={image} />
    
    <ClientRouter />
    
    <style>
      /* Custom view transition animations */
      ::view-transition-old(root),
      ::view-transition-new(root) {
        animation-duration: 0.3s;
      }
      
    </style>
  </head>
  <body>
    <Header lang={lang} isMainPage={isMainPage} />
    <main>
      <slot />
    </main>
  </body>
</html>

<style is:global>
  /* Reset y estilos base */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: 'Plus Jakarta Sans', sans-serif;
    background-color: white;
    color: black;
    line-height: 1.6;
  }
 
/* GitHub‑inspired styles for rendered Markdown */
.markdown-body {
  --md-text: #1a1a1a;
  --md-muted: #555;
  --md-border: #e1e4e8;
  --md-bg-code: #f6f8fa;
  --md-bg-blockquote: #f9fafb;

  font-size: 1rem;
  line-height: 1.7;
  color: var(--md-text);
  max-width: 100%;
  word-break: break-word;
  font-weight: 500;
}

.markdown-body h1,
.markdown-body h2,
.markdown-body h3,
.markdown-body h4,
.markdown-body h5,
.markdown-body h6 {
  margin-top: 1em;
  margin-bottom: 0.6em;
  font-weight: 700;
  line-height: 1.25;
}

.markdown-body h1 { font-size: 2rem;  }
.markdown-body h2 { font-size: 1.75rem; }
.markdown-body h3 { font-size: 1.5rem;  }
.markdown-body h4 { font-size: 1.25rem; }
.markdown-body h5 { font-size: 1.1rem;  }
.markdown-body h6 { font-size: 1rem;    font-weight: 600; }

.markdown-body p,
.markdown-body ul,
.markdown-body ol,
.markdown-body blockquote,
.markdown-body pre,
.markdown-body table {
  margin-bottom: 1.1em;
}

.markdown-body ul { list-style: disc inside; }
.markdown-body ol { list-style: decimal inside; }

.markdown-body a {
  color: #0366d6;
  text-decoration: underline;
  transition: color 0.15s;
}
.markdown-body a:hover { color: #024fbc; }

.markdown-body code {
  background: var(--md-bg-code);
  padding: 0.15em 0.35em;
  border-radius: 4px;
  font-size: 0.95em;
}

.markdown-body pre > code {
  display: block;
  padding: 1rem 1.25rem;
  overflow-x: auto;
}

.markdown-body pre {
  background: var(--md-bg-code);
  border: 1px solid var(--md-border);
  border-radius: 6px;
}

.markdown-body blockquote {
  background: var(--md-bg-blockquote);
  border-left: 4px solid var(--md-border);
  padding: 0.75em 1em;
  color: var(--md-muted);
  margin-left: 0;
}

.markdown-body table {
  width: 100%;
  border-collapse: collapse;
}

.markdown-body th,
.markdown-body td {
  border: 1px solid var(--md-border);
  padding: 0.5em 0.75em;
  text-align: left;
}

.markdown-body th {
  background: var(--md-bg-code);
  font-weight: 600;
}

.markdown-body hr {
  border: none;
  border-top: 1px solid var(--md-border);
  margin: 2em 0;
}

  /* Estilos para enlaces */
  a {
    color: inherit;
    text-decoration: none;
  }

  /* Estilos para imágenes */
  img {
    max-width: 100%;
    height: auto;
    display: block;
  }

  /* Estilos para botones */
  button {
    cursor: pointer;
    border: none;
    background: none;
    font-family: inherit;
  }

  /* Estilos para inputs */
  input, textarea {
    font-family: inherit;
    font-size: inherit;
  }

  /* Estilos para listas */
  ul, ol {
    list-style: none;
  }

  /* Estilos para secciones */
  section {
    margin-bottom: 15vh;
    width: 100%;
    position: relative;
    padding: 0rem;
  }

  section:first-child {
    margin-bottom: 0;
  }
  section:last-child {
    margin-bottom: 0;
  }
  
</style>
